import {Component, PropTypes} from 'react';

class Header extends Component {
    constructor(props) {
        super(props);
    }

    render() {
         return (
             <div></div>
         )
    }
}

Header.propTypes = {};

export default Header;