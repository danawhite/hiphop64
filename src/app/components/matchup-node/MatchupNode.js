import React from 'react';

const MatchupNode = ({seed, region}) => {
    return (
        <div>
            {this.props.seed}
            {this.props.region}
        </div>
        )
};

export default MatchupNode;